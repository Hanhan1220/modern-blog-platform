# 现代博客平台

一个基于 React、Supabase 和 Netlify 构建的现代化博客平台，支持文章发布、评论系统和标签管理。

## 🚀 功能特性

- 📝 **文章管理**: 支持创建、编辑、发布文章
- 💬 **评论系统**: 用户可以对文章进行评论
- 🏷️ **标签分类**: 文章标签管理和分类浏览
- 📱 **响应式设计**: 完美适配桌面和移动设备
- 🎨 **现代化 UI**: 基于 Tailwind CSS 的美观界面
- ⚡ **快速加载**: Vite 构建工具，极速开发体验
- 🔍 **SEO 友好**: 良好的搜索引擎优化

## 🛠️ 技术栈

### 前端
- **React 18** - 用户界面框架
- **Vite** - 构建工具
- **React Router** - 路由管理
- **Tailwind CSS** - 样式框架
- **Lucide React** - 图标库
- **React Markdown** - Markdown 渲染
- **date-fns** - 日期处理

### 后端
- **Supabase** - 数据库和后端服务
- **PostgreSQL** - 数据库

### 部署
- **Netlify** - 前端部署平台

## 📊 数据库设计

项目包含 3 个主要数据表：

### 1. posts（文章表）
- 存储文章标题、内容、摘要等
- 支持封面图片和发布状态
- 包含作者关联和统计信息

### 2. comments（评论表）
- 存储用户评论内容
- 支持嵌套评论结构
- 关联文章和作者信息

### 3. tags（标签表）
- 文章标签分类
- 支持标签颜色和描述
- 通过 post_tags 关联表与文章建立多对多关系

## 🚀 快速开始

### 1. 克隆项目

```bash
git clone <repository-url>
cd modern-blog-platform
```

### 2. 安装依赖

```bash
npm install
```

### 3. 配置环境变量

复制 `.env.example` 为 `.env` 并填入你的 Supabase 配置：

```bash
cp .env.example .env
```

编辑 `.env` 文件：

```env
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 4. 设置 Supabase 数据库

1. 在 [Supabase](https://supabase.com) 创建新项目
2. 在 SQL 编辑器中运行 `supabase-schema.sql` 文件
3. 获取项目 URL 和 anon key，填入环境变量

### 5. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:5173 查看应用。

## 📦 部署到 Netlify

### 1. 准备部署

确保项目已推送到 Git 仓库。

### 2. 连接 Netlify

1. 登录 [Netlify](https://netlify.com)
2. 点击 "New site from Git"
3. 选择你的 Git 仓库
4. 配置构建设置：
   - Build command: `npm run build`
   - Publish directory: `dist`

### 3. 设置环境变量

在 Netlify 控制台的 Site settings > Environment variables 中添加：

```
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 4. 部署

点击 "Deploy site" 开始部署。部署完成后，Netlify 会提供一个唯一的 URL。

## 📁 项目结构

```
modern-blog-platform/
├── public/                 # 静态资源
├── src/
│   ├── components/         # React 组件
│   │   ├── BlogCard.jsx
│   │   ├── CommentSection.jsx
│   │   └── Navbar.jsx
│   ├── contexts/          # React Context
│   │   └── SupabaseContext.jsx
│   ├── pages/             # 页面组件
│   │   ├── Home.jsx
│   │   ├── BlogDetail.jsx
│   │   ├── CreatePost.jsx
│   │   ├── Tags.jsx
│   │   └── TagDetail.jsx
│   ├── services/          # 服务层
│   │   └── blogService.js
│   ├── App.jsx            # 主应用组件
│   ├── main.jsx           # 应用入口
│   └── index.css          # 全局样式
├── supabase-schema.sql    # 数据库结构
├── netlify.toml          # Netlify 配置
├── tailwind.config.js    # Tailwind 配置
├── vite.config.js        # Vite 配置
└── package.json          # 项目配置
```

## 🎯 页面功能

### 1. 首页 (/)
- 展示最新文章列表
- 支持分页加载
- 响应式网格布局

### 2. 文章详情 (/blog/:id)
- 完整文章内容展示
- Markdown 渲染支持
- 评论系统
- 标签显示

### 3. 写文章 (/create)
- Markdown 编辑器
- 实时预览功能
- 标签选择
- 发布控制

### 4. 标签页面 (/tags)
- 标签云展示
- 标签统计信息
- 标签详情页面

## 🔧 开发指南

### 添加新功能

1. 在 `src/services/` 中添加数据服务
2. 在 `src/components/` 中创建 UI 组件
3. 在 `src/pages/` 中创建页面组件
4. 在 `App.jsx` 中添加路由

### 样式规范

- 使用 Tailwind CSS 类名
- 遵循组件化设计原则
- 保持响应式设计

### 代码规范

- 使用 ESLint 进行代码检查
- 遵循 React Hooks 最佳实践
- 保持组件单一职责

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 🙏 致谢

- [React](https://reactjs.org/) - 用户界面框架
- [Supabase](https://supabase.com/) - 后端即服务
- [Tailwind CSS](https://tailwindcss.com/) - CSS 框架
- [Netlify](https://netlify.com/) - 部署平台
- [Vite](https://vitejs.dev/) - 构建工具

## 📞 联系方式

如有问题或建议，请通过以下方式联系：

- 提交 Issue
- 发送邮件
- 项目讨论区

---

**注意**: 这是一个教学项目，展示了现代 Web 开发的最佳实践。在生产环境中使用前，请确保进行充分的安全测试。